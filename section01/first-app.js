const fs = require('fs'); // import file system

fs.writeFileSync('hello.txt', 'Hello World!'); // use filesystem to create a file called hello.txt if it doesn't exist, then write "Hello World!" to it.
